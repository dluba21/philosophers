#include "philo.h"

int main()
{
	pthread_mutex_t mutex;
	
//	mutex = malloc(sizeof(mutex));
	int a;
	pthread_mutex_init(&mutex, NULL);
//	if ( (a = pthread_mutex_destroy(&mutex)))
//	{
		printf("%d\n", pthread_mutex_destroy(&mutex));
//	}
	
}
